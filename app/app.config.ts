export default defineAppConfig({
  // Minimal, monochrome palette suited to an architecture portfolio.
  // Swap `primary` to an accent (e.g. 'amber', 'sky') to introduce colour.
  ui: {
    colors: {
      primary: 'neutral',
      neutral: 'stone'
    }
  }
})
